# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

git add src/components/AppLayout.tsx
git commit -m "refactor(layout): add shared app layout"

git add src/components/CustomLoader.tsx
git commit -m "refactor(loader): improve reusable loading component"

git add src/pages/Quiz/
git commit -m "refactor(quiz): migrate, modularize, and redesign quiz editor"

git add src/pages/analytics/
git commit -m "refactor(analytics): migrate and redesign analytics"

git add src/pages/quizInterface/
git commit -m "refactor(quiz-interface): migrate and redesign quiz interface"
